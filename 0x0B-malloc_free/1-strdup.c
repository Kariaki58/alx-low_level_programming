#include "main.h"
#include <string.h>
#include <stdlib.h>

/**
 * _strdup - duplicated a string
 * @str: string
 * Return: return dup of string
 */
char *_strdup(char *str)
{
	return (strdup(str));
}
