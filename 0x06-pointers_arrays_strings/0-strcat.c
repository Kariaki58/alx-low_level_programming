#include "main.h"
#include <string.h>

/**
 * _strcat - function to concatnate 2 string using strcat.
 * @dest: the string destination
 * @src: the string src
 * Return: return dest + src.
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
