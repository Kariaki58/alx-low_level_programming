#include "main.h"
#include <string.h>

/**
 * _strcmp - function to compare two string
 * @s1: string 1 to compare
 * @s2: string 2 to compare
 * Return: string 1 and string 2 if they are same
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
