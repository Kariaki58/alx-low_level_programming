#include <stdio.h>
/**
 * main - Entry point
 * Description: 'the program's description'
 * Return: Always 0 (Success)
 */
int main(void)
{
	char letter = 'a';
	int i = 0;

	while (i < 10)
	{
		putchar(i)
		i++;
	}
	while (letter <= 'f')
	{
		putchar(letter)
		letter++;
	}
	putchar('\n');
	return (0);
}
